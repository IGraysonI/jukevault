name: jukevault_platform_interface
description: A common platform interface for the jukevault plugin.
version: 0.0.1
homepage: https://github.com/IGraysonI/jukevault/packages/jukevault_platform_interface
publish_to: 'none'

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# flutter:
